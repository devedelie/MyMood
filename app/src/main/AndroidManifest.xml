<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elbaz.eliran.mymood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".model.CommentDialog"></activity>
        <activity android:name=".model.Statistics" />
        <activity android:name=".controller.SuperHappyMood" />
        <activity android:name=".controller.DisappointedMood" />
        <activity android:name=".controller.NormalMood" />
        <activity android:name=".controller.ReallyBadMoodActivity" />
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>